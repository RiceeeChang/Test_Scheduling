#ifndef LOWERBOUND
#define LOWERBOUND

#include <vector>
#include <algorithm>
#include "test.h"

using namespace std;

long int lowerbound(vector<Test*> tests, const int& width, const int& max_power);

#endif
